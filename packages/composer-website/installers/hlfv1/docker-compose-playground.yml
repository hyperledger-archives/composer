version: '2'

services:

  composer:
    container_name: composer
    image: hyperledger/composer-playground
    ports:
      - '8080:8080'
networks:
  default:
    external:
      name: composer_default
